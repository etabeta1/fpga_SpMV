- trovare la board su windows
- licensing problems
- vivado on hdd
- vm ubuntu
- cannot read from same mem port on two different dataflow processes
- better to not unroll loops with 2^18 iterations
- fatto con matrici tradizionali giusto per prenderci su la mano
https://www.youtube.com/watch?v=F9ZMiccYo8k
- no clean code (no templated top function (makes sense), no allocs with pretty structs (how do I pack structs from host code???), pointer hell, no safety)
- somehow c simulation crashes without error if you store 16 into an ap_uint<4> or just skips instructions
- arrow keys randomly stopping to work
- testbench generator
- overflow per numeri molto grandi --> -11585 < x < 11585 (oppure no)
- compiler flags, sanitizer
- #include <ap-int.h> makes cout not working; (or not)
- cout not working https://adaptivesupport.amd.com/s/question/0D52E00006hpPmfSAE/bug-print-value-in-testbench-from-hlsstream?language=en_US (lag??)
- asan crashing on infinite loop
- lots of ap_uint overflowing
- static variables to make registers and prevent lots of reads and writes to memory
- how transactions work
- never before seen files not found
- unused variables make cosim fail
- vivado
- change network settings to dhcp on home network (over serial)
- even if ap_int<X>, parameters as np.types




DOMANDE
- Il ruolo del depth e perchè non si può fare con le costanti (per dma di dimensione variabile??)
